# my-nvim-config

1. Put setting in .config/nvim
2. Install plug from [here](https://github.com/junegunn/vim-plug)
3. To be able use :CtrlSF, install ag first with `sudo apt-get install silversearcher-ag`
